[project]
name = "laptop-bootstrap"
version = "1.0.0"
description = "Ansible playbooks for laptop bootstrap and configuration"
requires-python = ">=3.10"
dependencies = [
    "ansible",
    "pre-commit",
    "ansible-lint",
    "requests",
    "docker",
]

[dependency-groups]
dev = [
    "yamllint",
]
